import { AuthenticationStatus } from './authentication-status.model';

describe('AuthenticationStatus', () => {
  it('should create an instance', () => {
    expect(new AuthenticationStatus()).toBeTruthy();
  });
});
